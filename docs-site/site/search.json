{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Mnemosynce","text":"<p>Mnemosynce is a backup orchestrator for Linux home servers. It runs a set of rsync tasks defined in a YAML config file, applies a tiered retention policy, syncs everything to a remote machine, and emails you a status report after every run — all from a self-hosted web interface.</p> <ul> <li> <p> User guide</p> <p>Install, configure, and operate Mnemosynce from the web UI. No prior Python knowledge needed.</p> <p> Get started</p> </li> <li> <p> Developer guide</p> <p>Understand the architecture, extend the code, and contribute. Covers every module, the test suite, and deployment.</p> <p> Architecture overview</p> </li> </ul>","path":["Mnemosynce"],"tags":[]},{"location":"#how-a-backup-run-works","level":2,"title":"How a backup run works","text":"<p>Each run executes three steps per configured task, in order:</p> <pre><code>flowchart LR\n    A([Source]) --&gt;|rsync| B[Backup\\nsnapshot]\n    B --&gt;|retention\\npolicy| C[Prune old\\nsnapshots]\n    C --&gt;|rsync over SSH| D([Remote\\nstore])\n    D --&gt; E{All tasks\\ndone?}\n    E --&gt;|yes| F[Email\\nreport]\n    E --&gt;|no| A</code></pre> Step What happens Backup <code>rsync</code> from source to a dated snapshot directory, using hard links so unchanged files cost no extra space Retention Removes snapshots outside the configured window (daily × 7, weekly × 4, monthly × 12, yearly × 5) Sync <code>rsync</code> the local backup directory to a remote machine over SSH <p>After all tasks finish, an HTML email report summarises what succeeded and what failed, with log files attached for any failed steps.</p>","path":["Mnemosynce"],"tags":[]},{"location":"developer/","level":1,"title":"Developer guide","text":"<p>This guide is for contributors and anyone who wants to understand, extend, or deploy Mnemosynce from source. It assumes familiarity with Python, Flask, and basic shell scripting.</p>","path":["Developer guide"],"tags":[]},{"location":"developer/#repository-layout","level":2,"title":"Repository layout","text":"<pre><code>mnemosynce/\n├── src/\n│   ├── backup_server/          # Core backup engine (no Flask dependency)\n│   │   ├── backup_task.py      # Orchestrates one task: backup → retention → sync\n│   │   ├── config_file.py      # Reads and validates backup_config.yml\n│   │   ├── database.py         # SQLite writer (LogDB)\n│   │   ├── email_report.py     # Composes and sends the HTML status email\n│   │   ├── logging_config.py   # JSON structured logging setup\n│   │   ├── main.py             # CLI entry point\n│   │   ├── backup.sh           # rsync snapshot script\n│   │   ├── delete_old_backups.sh     # Retention policy script\n│   │   ├── sync_backup_to_remote.sh  # Remote sync script\n│   │   └── templates/          # Jinja2 email templates\n│   ├── config/\n│   │   └── config.py           # Flask config classes (Dev / Test / Production)\n│   └── web/\n│       ├── app.py              # Flask application factory\n│       ├── auth.py             # Login/logout + login_required decorator\n│       ├── dashboard_data.py   # Read-only SQLite queries for the dashboard\n│       ├── run_state.py        # Thread-safe live run progress (SSE source)\n│       ├── scheduler.py        # APScheduler singleton + live log bridging\n│       ├── setup_guard.py      # setup_complete_required decorator\n│       ├── setup_state.py      # Session-backed readiness checks\n│       ├── routes/             # Flask blueprints (one file per feature area)\n│       │   ├── config_editor.py\n│       │   ├── connections.py\n│       │   ├── dashboard.py\n│       │   ├── main.py\n│       │   ├── progress.py\n│       │   ├── schedule.py\n│       │   ├── setup_wizard.py\n│       │   └── ssh_keys.py\n│       ├── static/             # CSS and images\n│       └── templates/web/      # Jinja2 page templates\n├── backup.sh                   # rsync snapshot script\n├── delete_old_backups.sh       # Retention policy script\n├── sync_backup_to_remote.sh    # Remote sync script\n├── tests/                      # pytest suite\n├── docker/Dockerfile\n├── pyproject.toml\n└── docs-site/                  # This documentation\n</code></pre>","path":["Developer guide"],"tags":[]},{"location":"developer/#key-design-decisions","level":2,"title":"Key design decisions","text":"<p>Backup engine is Flask-free. <code>src/backup_server/</code> has no Flask imports. It can be run directly from the CLI (<code>python -m backup_server.main config.yml</code>) or called from the web scheduler. This keeps the engine independently testable and deployable.</p> <p>Single SQLite database, two access paths. <code>LogDB</code> (in <code>backup_server/database.py</code>) writes run results; <code>dashboard_data.py</code> (in <code>web/</code>) reads them with separate read-only queries. Both access the same <code>log.db</code> file but are never imported together.</p> <p>Live progress via SSE. <code>RunState</code> in <code>run_state.py</code> is a module-level singleton shared between the scheduler thread (writer) and the Flask SSE endpoint (reader). A <code>threading.Lock</code> guards all mutations. The SSE stream in <code>progress.py</code> polls <code>RunState</code> at 300 ms intervals and pushes <code>line</code> and <code>state</code> events to the browser.</p> <p>Setup state is session-scoped. The four readiness checks are re-derived from the filesystem on every request. Only the connection-test result is stored in the Flask session (because it can't be inferred from disk). This means no extra database tables and no migration concerns, at the cost of re-running the wizard after a server restart if setup wasn't complete.</p>","path":["Developer guide"],"tags":[]},{"location":"developer/architecture/","level":1,"title":"Architecture","text":"","path":["Developer guide","Architecture"],"tags":[]},{"location":"developer/architecture/#component-map","level":2,"title":"Component map","text":"<pre><code>flowchart TD\n    subgraph Browser\n        UI[Web UI\\nBootstrap 5 + SSE]\n    end\n\n    subgraph Flask[\"Flask application (src/web/)\"]\n        APP[app.py\\nfactory]\n        AUTH[auth.py]\n        GUARD[setup_guard.py]\n        STATE[setup_state.py]\n        SCHED[scheduler.py\\nAPScheduler]\n        RS[run_state.py\\nRunState singleton]\n        ROUTES[Blueprint routes]\n    end\n\n    subgraph Engine[\"Backup engine (src/backup_server/)\"]\n        MAIN[main.py]\n        TASK[backup_task.py]\n        CFG[config_file.py]\n        DB[database.py\\nLogDB]\n        EMAIL[email_report.py]\n        SH[\"Shell scripts\\nbackup.sh\\ndelete_old_backups.sh\\nsync_backup_to_remote.sh\"]\n    end\n\n    subgraph Storage[\"Persistent storage (/data)\"]\n        YML[backup_config.yml]\n        SQLITE[(log.db)]\n        JSON[schedule.json]\n        KEYS[ssh/]\n    end\n\n    UI --&gt;|HTTP / SSE| ROUTES\n    ROUTES --&gt; AUTH\n    ROUTES --&gt; GUARD\n    GUARD --&gt; STATE\n    STATE --&gt;|reads| YML\n    STATE --&gt;|reads| JSON\n    STATE --&gt;|reads| KEYS\n    STATE --&gt;|session| Browser\n\n    SCHED --&gt;|calls| MAIN\n    MAIN --&gt; CFG --&gt; YML\n    MAIN --&gt; TASK --&gt; SH\n    MAIN --&gt; DB --&gt; SQLITE\n    MAIN --&gt; EMAIL\n\n    SCHED --&gt;|pushes lines| RS\n    RS --&gt;|SSE stream| UI\n\n    ROUTES --&gt;|reads| SQLITE</code></pre>","path":["Developer guide","Architecture"],"tags":[]},{"location":"developer/architecture/#request-lifecycle","level":2,"title":"Request lifecycle","text":"<p>A typical page request goes through four layers:</p> <ol> <li><code>@login_required</code> — redirects to <code>/login</code> if the session has no <code>logged_in</code> flag (production only; bypassed in development).</li> <li><code>@setup_complete_required</code> — redirects to <code>/setup/</code> if <code>is_setup_complete()</code> returns False. Applied to all operational routes (dashboard, progress, schedule run/status).</li> <li>Route handler — fetches data, renders template.</li> <li>Context processor (<code>inject_setup_status</code>) — injects <code>setup_status</code> and <code>setup_complete</code> into every template so <code>base.html</code> can render the correct navbar without per-route logic.</li> </ol>","path":["Developer guide","Architecture"],"tags":[]},{"location":"developer/architecture/#threading-model","level":2,"title":"Threading model","text":"<p>The Flask development server and Gunicorn (with <code>--workers 2</code>) are both multi-threaded. Two threads can be active simultaneously:</p> Thread Writer Reader Backup runner (APScheduler or manual) <code>RunState.add_line()</code>, <code>RunState.step_*()</code> — Flask SSE handler (<code>/progress/stream</code>) — <code>RunState.lines_from()</code>, <code>RunState.snapshot()</code> <p><code>RunState</code> uses a <code>threading.Lock</code> on every mutation and read. The lock is held only for the duration of a list slice or attribute set — never across I/O — so contention is negligible.</p>","path":["Developer guide","Architecture"],"tags":[]},{"location":"developer/architecture/#data-flow-a-scheduled-backup-run","level":2,"title":"Data flow: a scheduled backup run","text":"<pre><code>sequenceDiagram\n    participant APSched as APScheduler\n    participant Sched as scheduler.py\n    participant RS as RunState\n    participant Engine as backup_server.main\n    participant DB as log.db\n    participant SSE as /progress/stream\n    participant Browser\n\n    APSched-&gt;&gt;Sched: trigger _run_backup(app)\n    Sched-&gt;&gt;RS: state.start(task_name)\n    Sched-&gt;&gt;Engine: main(config, password)\n    loop per log record\n        Engine--&gt;&gt;Sched: _StateHandler.emit(record)\n        Sched-&gt;&gt;RS: state.add_line(msg)\n        Sched-&gt;&gt;RS: state.step_running/done(...)\n    end\n    Engine-&gt;&gt;DB: LogDB.add_task_run(status)\n    Engine--&gt;&gt;Sched: return\n    Sched-&gt;&gt;RS: state.finish(success)\n\n    loop SSE poll (300 ms)\n        SSE-&gt;&gt;RS: lines_from(seq)\n        RS--&gt;&gt;SSE: new lines + seq\n        SSE--&gt;&gt;Browser: event: line / state\n    end</code></pre>","path":["Developer guide","Architecture"],"tags":[]},{"location":"developer/backup-engine/","level":1,"title":"Backup engine","text":"<p>The backup engine (<code>src/backup_server/</code>) is a self-contained Python package with no Flask dependency. It can be invoked from the command line or called programmatically by the web scheduler.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#entry-point-mainpy","level":2,"title":"Entry point — <code>main.py</code>","text":"<pre><code>main(file_config=\"config.yml\", password_reader=_read_password)\n</code></pre> <p><code>main()</code> ties everything together:</p> <ol> <li>Reads the Gmail password via <code>password_reader</code> (injectable for tests).</li> <li>Opens a <code>LogDB</code> connection and runs each configured task in sequence.</li> <li>Passes all task statuses to <code>EmailReport.send_mail()</code>.</li> <li>Calls <code>delete_logs()</code> to remove per-step log files.</li> </ol> <p>The <code>password_reader</code> parameter exists solely to make tests independent of the real filesystem and nix-sops secrets.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#configfile-config_filepy","level":2,"title":"<code>ConfigFile</code> — <code>config_file.py</code>","text":"<p><code>ConfigFile(file_config).read()</code> loads and validates <code>backup_config.yml</code>. Validation checks:</p> <ul> <li>File exists.</li> <li>All required top-level keys are present (<code>dir_backup_local</code>, <code>dir_backup_remote</code>, <code>email_sender</code>, <code>email_report</code>, <code>tasks</code>).</li> <li>Each task has <code>name</code> and <code>dir_source</code>.</li> <li><code>email_admin</code> is normalised to <code>\"\"</code> when absent or equal to <code>email_report</code>.</li> </ul> <p>Raises <code>FileNotFoundError</code> or <code>KeyError</code> on validation failure — the web config editor runs the same checks before saving and surfaces them as flash messages.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#backuptask-backup_taskpy","level":2,"title":"<code>BackupTask</code> — <code>backup_task.py</code>","text":"<p><code>BackupTask(task, dir_local, dir_remote).start()</code> runs the three-step workflow for a single task and returns a status dictionary.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#step-execution","level":3,"title":"Step execution","text":"<p>Each step is run via <code>subprocess.run</code> (injectable via the <code>runner</code> parameter for tests). Steps run in order; if one fails the subsequent steps are skipped.</p> Step Script Key rsync flags Backup <code>backup.sh</code> <code>-az --delete --hard-links --exclude-from=excludes.lst</code> Retention <code>delete_old_backups.sh</code> Shell date arithmetic — no rsync Sync <code>sync_backup_to_remote.sh</code> <code>-azhtH --numeric-ids --delete -e ssh -i /root/.ssh/id_ed25519_backup</code>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#error-handling","level":3,"title":"Error handling","text":"<p><code>_stderr_has_error()</code> filters rsync stderr to distinguish real failures from known non-fatal warnings:</p> <ul> <li><code>Permission denied (13)</code> — treated as a warning (rsync exit 23)</li> <li><code>some files/attrs were not transferred</code> — treated as a warning</li> <li>Anything else in stderr with a non-zero exit code — fatal</li> </ul>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#status-dictionary","level":3,"title":"Status dictionary","text":"<p><code>start()</code> returns a dict shaped like:</p> <pre><code>{\n    \"name\": \"ServerData\",\n    \"success\": True,\n    \"dt_task_start\": 1714000000.0,\n    \"dt_task_end\":   1714000120.0,\n    \"steps\": [\n        {\n            \"step\": \"backup\",\n            \"success\": True,\n            \"dt_start\": 1714000000.0,\n            \"dt_end\":   1714000060.0,\n            \"time_elapsed\": \"00:01:00\",\n            \"dir_from\": \"/data\",\n            \"dir_to\":   \"/mnt/backup/local/ServerData/2026-04-19\",\n            \"file_log\": Path(\"ServerData_backup.log\"),\n        },\n        # ... retention, sync\n    ],\n}\n</code></pre>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#logdb-databasepy","level":2,"title":"<code>LogDB</code> — <code>database.py</code>","text":"<p>A thin SQLite wrapper using a single <code>task_run</code> table. Each row represents one step of one task run:</p> Column Type Description <code>id_task</code> TEXT Task name <code>dt_task_start</code> REAL Unix timestamp of task start <code>dt_task_end</code> REAL Unix timestamp of task end <code>success_task</code> INTEGER 1 if all steps succeeded <code>id_step</code> TEXT <code>backup</code>, <code>retention</code>, or <code>sync</code> <code>dt_step_start</code> REAL Step start timestamp <code>dt_step_end</code> REAL Step end timestamp <code>success_step</code> INTEGER 1 if this step succeeded <code>dir_from</code> TEXT Source path <code>dir_to</code> TEXT Destination path <code>time_elapsed</code> TEXT <code>HH:MM:SS</code> string <p><code>LogDB</code> is used as a context manager (<code>with LogDB(...) as db:</code>). The web dashboard reads the same database via the separate <code>dashboard_data.py</code> module.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#emailreport-email_reportpy","level":2,"title":"<code>EmailReport</code> — <code>email_report.py</code>","text":"<p><code>EmailReport.send_mail(lst_task_status)</code> enriches the raw status dicts, renders HTML and plain-text bodies via Jinja2, attaches any failed-step log files as zips, and sends via Gmail SMTP SSL on port 465.</p> <p><code>enrich_task_status()</code> is a pure function (separated from the class) that:</p> <ul> <li>Adds <code>time_task_elapsed</code>, formatted <code>dt_task_start</code>/<code>dt_task_end</code>.</li> <li>Looks up the last successful run per task from <code>LogDB</code> and sets <code>days_since_last_success</code>.</li> <li>Fills in any steps that were never reached (so templates always see all three steps).</li> </ul>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/backup-engine/#shell-scripts","level":2,"title":"Shell scripts","text":"<p>The three bash scripts live in <code>src/backup_server/</code> alongside the Python modules and are resolved via the module-level <code>_SCRIPTS_DIR = Path(__file__).parent</code> constant in <code>backup_task.py</code>. They are included in the installed package via the <code>*.sh</code> glob in <code>pyproject.toml</code>'s <code>[tool.setuptools.package-data]</code> section.</p> Script Arguments Purpose <code>backup.sh</code> <code>NAME DIR_BACKUP DIR_SOURCE</code> rsync snapshot with hard-link deduplication <code>delete_old_backups.sh</code> <code>NAME DIR_BACKUP</code> Prune snapshots outside the retention window <code>sync_backup_to_remote.sh</code> <code>NAME DIR_LOCAL DIR_REMOTE</code> rsync to remote storage over SSH <p>Note</p> <p>The scripts write per-step log files (<code>&lt;name&gt;_backup.log</code>, etc.) to the current working directory. <code>main.py</code> deletes these after the email is sent.</p>","path":["Developer guide","Backup engine"],"tags":[]},{"location":"developer/config-reference/","level":1,"title":"Configuration reference","text":"<p>Flask configuration is handled by three config classes in <code>src/config/config.py</code>, selected by the <code>APP_ENV</code> environment variable.</p>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#config-classes","level":2,"title":"Config classes","text":"<pre><code>classDiagram\n    class BaseConfig {\n        +APP_ENV: str\n        +DATA_ROOT: Path\n        +CONFIG_PATH: Path\n        +DB_PATH: Path\n        +SSH_KEY_DIR: Path\n        +SECRET_KEY: str\n        +ADMIN_USER: str\n        +ADMIN_PASSWORD: str\n        +GMAIL_ADDRESS: str\n        +GMAIL_PASSWORD: str\n        +ensure_dirs()\n    }\n    class DevelopmentConfig {\n        +DEBUG: bool = True\n        +DATA_ROOT: Path = dev-data/\n    }\n    class TestConfig {\n        +DEBUG: bool = True\n        +TESTING: bool = True\n        +DATA_ROOT: Path = /tmp/backup-server-test-data\n        +ADMIN_PASSWORD: str = test-password\n        +SECRET_KEY: str = test-secret\n    }\n    class ProductionConfig {\n        +DEBUG: bool = False\n        +__init__() raises if defaults unchanged\n    }\n\n    BaseConfig &lt;|-- DevelopmentConfig\n    BaseConfig &lt;|-- TestConfig\n    BaseConfig &lt;|-- ProductionConfig</code></pre> <p>Selected by <code>get_config()</code>:</p> <pre><code>env = os.getenv(\"APP_ENV\", \"development\")\n# \"development\" → DevelopmentConfig\n# \"test\"        → TestConfig\n# \"production\"  → ProductionConfig\n</code></pre>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#environment-variables","level":2,"title":"Environment variables","text":"<p>All configuration values are read from environment variables (or from <code>.env</code> in development via <code>python-dotenv</code>).</p>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#required-in-production","level":3,"title":"Required in production","text":"Variable Description <code>SECRET_KEY</code> Flask session signing key. Must be a long random string. <code>ProductionConfig.__init__()</code> raises <code>RuntimeError</code> if still set to the default placeholder. <code>ADMIN_PASSWORD</code> Web UI admin password. Same guard as <code>SECRET_KEY</code>.","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#optional","level":3,"title":"Optional","text":"Variable Default Description <code>APP_ENV</code> <code>development</code> Selects the config class. <code>ADMIN_USER</code> <code>admin</code> Web UI username. <code>GMAIL_ADDRESS</code> <code>\"\"</code> Gmail sender address, used as SMTP username. <code>GMAIL_PASSWORD</code> <code>\"\"</code> Gmail app password (or the content of <code>GMAIL_PASSWORD_FILE</code>). <code>DATA_ROOT</code> <code>/data</code> (production), <code>dev-data/</code> (development) Root directory for all persistent data.","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#gmail_password_file-cli-mode-only","level":3,"title":"<code>GMAIL_PASSWORD_FILE</code> (CLI mode only)","text":"<p>When running the backup engine directly from the command line, the Gmail password is read from a file, not an env var directly:</p> <pre><code>export GMAIL_PASSWORD_FILE=/etc/mnemosynce/gmail-password\nuv run python -m backup_server.main config.yml\n</code></pre> <p>This is handled by <code>_read_password()</code> in <code>main.py</code>. The web app reads the password directly from <code>GMAIL_PASSWORD</code> in the Flask config instead.</p>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#derived-paths","level":2,"title":"Derived paths","text":"<p><code>BaseConfig</code> derives three paths from <code>DATA_ROOT</code>. Override <code>DATA_ROOT</code> to move all state to a different location; the derived paths follow automatically.</p> Attribute Derived as Purpose <code>CONFIG_PATH</code> <code>DATA_ROOT / \"backup_config.yml\"</code> Backup task definitions <code>DB_PATH</code> <code>DATA_ROOT / \"log.db\"</code> SQLite run history <code>SSH_KEY_DIR</code> <code>DATA_ROOT / \"ssh\"</code> Generated SSH keypairs <p><code>ensure_dirs()</code> creates <code>DATA_ROOT</code> and <code>SSH_KEY_DIR</code> if they do not exist. Called once at application startup by <code>create_app()</code>.</p>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#development-mode-behaviour","level":2,"title":"Development mode behaviour","text":"<p>When <code>APP_ENV=development</code>:</p> <ul> <li><code>login_required</code> is a no-op — all routes are accessible without a session.</li> <li><code>DATA_ROOT</code> defaults to <code>dev-data/</code> inside the project root.</li> <li><code>DEBUG=True</code> enables the Werkzeug reloader and detailed error pages.</li> <li><code>.env</code> is loaded automatically from the project root by <code>python-dotenv</code>.</li> </ul>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/config-reference/#adding-a-new-config-value","level":2,"title":"Adding a new config value","text":"<ol> <li>Add a class attribute to <code>BaseConfig</code> with an <code>os.getenv(...)</code> default.</li> <li>Override it in <code>DevelopmentConfig</code> and <code>TestConfig</code> as needed.</li> <li>If the value must not be left as a placeholder in production, add a guard to <code>ProductionConfig.__init__()</code>.</li> <li>Access it in Flask code via <code>current_app.config[\"MY_VALUE\"]</code> or <code>app.config[\"MY_VALUE\"]</code>.</li> </ol> <pre><code># In BaseConfig\nMY_VALUE: str = os.getenv(\"MY_VALUE\", \"default\")\n\n# In ProductionConfig.__init__\nif self.MY_VALUE == \"default\":\n    raise RuntimeError(\"MY_VALUE must be set in production.\")\n</code></pre>","path":["Developer guide","Configuration reference"],"tags":[]},{"location":"developer/setup-wizard/","level":1,"title":"Setup wizard internals","text":"<p>The setup wizard is implemented across three modules that together answer one question on every request: has this user finished setting up the application?</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#module-responsibilities","level":2,"title":"Module responsibilities","text":"Module Responsibility <code>setup_state.py</code> Derives readiness from disk + session; exposes <code>get_setup_status()</code> and <code>is_setup_complete()</code> <code>setup_guard.py</code> <code>setup_complete_required</code> decorator — redirects to <code>/setup/</code> when incomplete <code>routes/setup_wizard.py</code> Flask blueprint — four step pages + <code>POST /setup/complete</code>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#setup_statepy","level":2,"title":"<code>setup_state.py</code>","text":"","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#the-four-checks","level":3,"title":"The four checks","text":"<p>Each check is evaluated freshly on every call to <code>get_setup_status()</code>. Checks 1, 2, and 4 read the filesystem; check 3 reads the Flask session.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#1-config","level":4,"title":"1. <code>config</code>","text":"<p><code>_check_config(app)</code> returns <code>True</code> when:</p> <ul> <li><code>CONFIG_PATH</code> exists, and</li> <li>it parses as valid YAML, and</li> <li>all five required keys are present: <code>dir_backup_local</code>, <code>dir_backup_remote</code>, <code>email_sender</code>, <code>email_report</code>, <code>tasks</code>.</li> </ul> <p>The same structural check is used by the config editor route before saving — the web UI and the setup state are always in agreement.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#2-ssh_key","level":4,"title":"2. <code>ssh_key</code>","text":"<p><code>_check_ssh_key(app)</code> returns <code>True</code> when at least one <code>*.pub</code> file in <code>SSH_KEY_DIR</code> has a matching private key file alongside it.</p> <p>Auto-satisfied when <code>has_remote_sources</code> is <code>False</code> (all task sources are local paths).</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#3-connection","level":4,"title":"3. <code>connection</code>","text":"<p><code>_connection_tested()</code> returns <code>True</code> when <code>session[\"setup_connection_tested\"]</code> is truthy.</p> <p>This flag is written by <code>mark_connection_tested()</code>, which is called from <code>connections.test_ssh</code> and <code>connections.test_email</code> after any successful test.</p> <p>Auto-satisfied when <code>has_remote_sources</code> is <code>False</code>.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#4-schedule","level":4,"title":"4. <code>schedule</code>","text":"<p><code>_check_schedule(app)</code> returns <code>True</code> when <code>DATA_ROOT/schedule.json</code> exists and its <code>\"enabled\"</code> field is truthy.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#has_remote_sources","level":3,"title":"<code>has_remote_sources</code>","text":"<p><code>_has_remote_sources(app)</code> scans the <code>tasks</code> list in the saved config and returns <code>True</code> if any <code>dir_source</code> contains <code>@</code> (i.e. <code>user@host:/path</code> form). This drives the skip logic for steps 2 and 3.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#completion-logic","level":3,"title":"Completion logic","text":"<pre><code>complete = (config_ok and ssh_ok and connection_ok and schedule_ok) \\\n           or session.get(\"setup_explicitly_complete\")\n</code></pre> <p>Two independent paths reach <code>complete = True</code>:</p> <ul> <li>Automatic — all four checks pass simultaneously (re-evaluated on every request).</li> <li>Explicit — the user clicked Finish setup anyway, which sets <code>session[\"setup_explicitly_complete\"]</code> via <code>mark_setup_complete()</code>.</li> </ul>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#session-scope","level":3,"title":"Session scope","text":"<p>Because completion state lives in the browser session, it resets when:</p> <ul> <li>The server restarts (Flask sessions are signed with <code>SECRET_KEY</code> but not server-side — losing the key invalidates all sessions).</li> <li>The user logs out (<code>session.clear()</code>).</li> <li>The cookie expires.</li> </ul> <p>After a reset, <code>is_setup_complete()</code> re-evaluates the filesystem checks. If config, SSH key, and schedule are all still on disk, setup is automatically complete again — the only check that requires re-doing is the connection test (which has no on-disk representation).</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#setup_guardpy","level":2,"title":"<code>setup_guard.py</code>","text":"<pre><code>@setup_complete_required\ndef my_view():\n    ...\n</code></pre> <p>The decorator calls <code>is_setup_complete(app)</code> and redirects to <code>url_for(\"setup.index\")</code> if it returns <code>False</code>. It is applied after <code>@login_required</code> in the decorator stack, so the evaluation order is:</p> <pre><code>request → @login_required → @setup_complete_required → view function\n</code></pre> <p>Unauthenticated users hit the login wall before the setup check — they never see the wizard URL directly.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#which-routes-are-gated","level":3,"title":"Which routes are gated","text":"Route Gated <code>dashboard.index</code>, <code>dashboard.history</code> Yes <code>progress.index</code>, <code>progress.state_json</code>, <code>progress.stream</code> Yes <code>schedule.index</code>, <code>schedule.run_now</code>, <code>schedule.status</code> Yes <code>schedule.save</code>, <code>schedule.remove</code> No — wizard step 4 uses these <code>config_editor.*</code>, <code>connections.*</code>, <code>ssh_keys.*</code> No — used during wizard <code>setup.*</code> No — would cause a redirect loop","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#routessetup_wizardpy","level":2,"title":"<code>routes/setup_wizard.py</code>","text":"","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#blueprint","level":3,"title":"Blueprint","text":"<p>Registered at <code>/setup</code>. All routes require <code>@login_required</code> but are explicitly excluded from <code>setup_complete_required</code>.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#step-routing","level":3,"title":"Step routing","text":"<p><code>/setup/</code> (index) reads <code>get_setup_status()</code>, builds the list of visible steps via <code>_visible_steps(has_remote)</code>, finds the first incomplete one, and redirects there. If setup is already complete it redirects to the dashboard.</p> <p>Steps 2 and 3 (<code>/setup/ssh-keys</code> and <code>/setup/connections</code>) redirect themselves to <code>/setup/schedule</code> when <code>has_remote_sources</code> is <code>False</code> — they never render.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#next-parameter","level":3,"title":"<code>?next=</code> parameter","text":"<p>Wizard step templates post forms to the existing route handlers for config, SSH keys, and schedule, not to new wizard-specific endpoints. Each form action includes <code>?next=&lt;current-wizard-step-url&gt;</code>:</p> <pre><code>&lt;form method=\"post\"\n      action=\"{{ url_for('config_editor.save') }}?next={{ url_for('setup.step_config') }}\"&gt;\n</code></pre> <p>The underlying route handlers (<code>config_editor.save</code>, <code>ssh_keys.generate</code>, <code>ssh_keys.delete</code>, <code>schedule.save</code>, <code>schedule.remove</code>) honour this parameter:</p> <pre><code>next_url = request.args.get(\"next\") or url_for(\"config_editor.editor\")\nreturn redirect(next_url)\n</code></pre> <p>This avoids duplicating the save/validate logic in the wizard and keeps the standalone pages fully functional at the same time.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#finish-endpoint","level":3,"title":"Finish endpoint","text":"<p><code>POST /setup/complete</code> calls <code>mark_setup_complete()</code> and redirects to the dashboard. It is the target of the Finish setup and Finish setup anyway buttons on the Schedule step.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#stepper-state","level":3,"title":"Stepper state","text":"<p>Each step template extends <code>wizard_base.html</code>, which renders the horizontal stepper bar. The stepper receives:</p> Variable Type Source <code>wizard_steps</code> <code>list[dict]</code> <code>_visible_steps(has_remote_sources)</code> <code>wizard_current</code> <code>int</code> <code>_current_step_index(status, steps)</code> <code>setup_status</code> <code>dict</code> <code>get_setup_status(app)</code> <p>Steps before <code>wizard_current</code> render with a green check bubble; the active step renders with a blue outlined bubble; future steps render grey.</p>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup-wizard/#adding-a-new-setup-step","level":2,"title":"Adding a new setup step","text":"<ol> <li>Add an entry to <code>WIZARD_STEPS</code> in <code>routes/setup_wizard.py</code> with a unique <code>id</code>, <code>label</code>, <code>icon</code>, and <code>route</code>.</li> <li>Add the corresponding check to <code>setup_state.py</code> — a filesystem or session check that returns a <code>bool</code>.</li> <li>Include the new <code>id</code> in the <code>complete</code> calculation in <code>get_setup_status()</code>.</li> <li>Create the route function and template (extending <code>wizard_base.html</code>, filling <code>{% block wizard_content %}</code>).</li> <li>If the step can be skipped under certain conditions, add the skip logic inside the route function (redirect to the next step's URL).</li> <li>Add tests in <code>tests/test_setup_wizard.py</code> covering the new check and the HTTP behaviour.</li> </ol>","path":["Developer guide","Setup wizard internals"],"tags":[]},{"location":"developer/setup/","level":1,"title":"Developer setup","text":"","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#prerequisites","level":2,"title":"Prerequisites","text":"<ul> <li>Python 3.12+</li> <li><code>uv</code> — fast Python package manager</li> <li><code>rsync</code> and <code>openssh-client</code> on your PATH (for functional tests only)</li> </ul>","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#clone-and-install","level":2,"title":"Clone and install","text":"<pre><code>git clone https://github.com/mark-me/backup-server.git\ncd backup-server\nuv sync --extra dev\n</code></pre> <p>This installs the application dependencies plus the dev extras: <code>pytest</code>, <code>pytest-cov</code>, <code>mypy</code>, and <code>ruff</code>.</p>","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#environment","level":2,"title":"Environment","text":"<pre><code>cp .env.example .env\n# Edit .env — the defaults work for development\n</code></pre> .env<pre><code>APP_ENV=development        # disables login checks\nSECRET_KEY=change-me-locally\nADMIN_USER=admin\nADMIN_PASSWORD=dev-password\nGMAIL_ADDRESS=your.account@gmail.com\nGMAIL_PASSWORD=your-app-password\nDATA_ROOT=dev-data         # created automatically\n</code></pre> <p>In <code>development</code> mode:</p> <ul> <li>All routes are accessible without logging in</li> <li><code>DATA_ROOT</code> defaults to <code>dev-data/</code> in the project root</li> <li>Debug mode is on (auto-reload on code changes)</li> </ul>","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#run-the-dev-server","level":2,"title":"Run the dev server","text":"<pre><code>uv run flask --app src/web/app:create_app run --host 0.0.0.0 --port 5000\n</code></pre> <p>Or use the VS Code launch configuration in <code>.vscode/launch.json</code>.</p>","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#code-style","level":2,"title":"Code style","text":"<pre><code>uv run ruff check src/ tests/      # lint\nuv run ruff format src/ tests/     # format\nuv run mypy src/                   # type check\n</code></pre> <p>Ruff is configured in <code>pyproject.toml</code> with <code>line-length = 100</code> and the <code>E</code>, <code>F</code>, <code>I</code>, <code>UP</code> rule sets.</p>","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/setup/#project-dependencies","level":2,"title":"Project dependencies","text":"<p>Core runtime dependencies (from <code>pyproject.toml</code>):</p> Package Purpose <code>flask</code> Web framework <code>gunicorn</code> Production WSGI server <code>APScheduler</code> Cron-style background job scheduler <code>PyYAML</code> Parsing <code>backup_config.yml</code> <code>jinja2</code> Email and HTML templating <code>python-dotenv</code> Loading <code>.env</code> in development <code>python-json-logger</code> Structured JSON logging","path":["Developer guide","Developer setup"],"tags":[]},{"location":"developer/testing/","level":1,"title":"Testing","text":"<p>Mnemosynce uses <code>pytest</code>. All tests live in <code>tests/</code> and run without a real backup server, real SSH connections, or real SMTP.</p>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#running-the-tests","level":2,"title":"Running the tests","text":"<pre><code># Fast — no real scripts or network\nuv run pytest -v -m \"not functional\"\n\n# With coverage\nuv run pytest -v -m \"not functional\" --cov=src --cov-report=term-missing\n\n# Functional tests — requires the project to be fully set up on a real machine\nuv run pytest -v -m functional\n</code></pre> <p>The CI pipeline (<code>.github/workflows/test.yml</code>) runs <code>pytest -m \"not functional\"</code>.</p>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#test-files","level":2,"title":"Test files","text":"File Covers <code>test_backup_task.py</code> <code>BackupTask</code> — excludes file, subprocess runner, step status, error handling <code>test_config_file.py</code> <code>ConfigFile</code> — valid config, missing keys, optional fields <code>test_database.py</code> <code>LogDB</code> — table creation, inserting runs, last-success query <code>test_email_report.py</code> <code>enrich_task_status</code>, <code>EmailReport._compose_mail</code>, SMTP send, attachments <code>test_main.py</code> <code>main()</code> end-to-end with fakes, log cleanup, password reader <code>test_setup_wizard.py</code> <code>setup_state</code>, <code>setup_guard</code>, wizard routes <code>test_functional.py</code> Shell scripts (marked <code>functional</code>, excluded from CI)","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#fixtures-conftestpy","level":2,"title":"Fixtures — <code>conftest.py</code>","text":"","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#minimal_config","level":3,"title":"<code>minimal_config</code>","text":"<p>Writes a valid <code>backup_config.yml</code> to <code>tmp_path</code> and returns its <code>Path</code>. Used by backup engine tests that need a real file on disk.</p> <pre><code>@pytest.fixture()\ndef minimal_config(tmp_path):\n    cfg = {\"dir_backup_local\": ..., \"tasks\": [...], ...}\n    p = tmp_path / \"config.yml\"\n    p.write_text(yaml.dump(cfg))\n    return p\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#fake_runner","level":3,"title":"<code>fake_runner</code>","text":"<p>Returns a factory for constructing <code>subprocess.CompletedProcess</code> objects with configurable <code>returncode</code>, <code>stdout</code>, and <code>stderr</code>. Injected into <code>BackupTask</code> via the <code>runner</code> parameter.</p> <pre><code>@pytest.fixture()\ndef fake_runner():\n    def make(returncode=0, stdout=\"\", stderr=\"\"):\n        def runner(*args, **kwargs):\n            return subprocess.CompletedProcess(args, returncode, stdout, stderr)\n        return runner\n    return make\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#app-and-client-wizard-tests","level":3,"title":"<code>app</code> and <code>client</code> (wizard tests)","text":"<p>The wizard test fixtures create a <code>TestConfig</code> with all paths pointing to <code>tmp_path</code>, construct the Flask app, and return both the app and a <code>test_client()</code>. This keeps every test fully isolated — no shared filesystem state.</p> <pre><code>@pytest.fixture()\ndef app(tmp_path):\n    cfg = TestConfig()\n    cfg.DATA_ROOT   = tmp_path\n    cfg.CONFIG_PATH = tmp_path / \"backup_config.yml\"\n    cfg.DB_PATH     = tmp_path / \"log.db\"\n    cfg.SSH_KEY_DIR = tmp_path / \"ssh\"\n    cfg.SSH_KEY_DIR.mkdir()\n    yield create_app(cfg)\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#testing-patterns","level":2,"title":"Testing patterns","text":"","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#injecting-a-fake-subprocess-runner","level":3,"title":"Injecting a fake subprocess runner","text":"<p><code>BackupTask</code> accepts a <code>runner</code> parameter so tests never shell out:</p> <pre><code>def test_start_full_success(fake_runner, tmp_path):\n    task = BackupTask(\n        task={\"name\": \"T\", \"dir_source\": \"/src\"},\n        dir_local=str(tmp_path),\n        dir_remote=\"user@host:/remote\",\n        work_dir=tmp_path,\n        runner=fake_runner(returncode=0),\n    )\n    status = task.start()\n    assert status[\"success\"] is True\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#testing-flask-routes-with-test_client","level":3,"title":"Testing Flask routes with <code>test_client</code>","text":"<p>The wizard tests use <code>client.get()</code> and <code>client.post()</code> against a fully wired Flask app in <code>TestConfig</code> mode. Because <code>APP_ENV</code> is not <code>\"development\"</code> in <code>TestConfig</code>, <code>login_required</code> would normally enforce authentication — but <code>TestConfig</code> sets <code>TESTING = True</code>, which the <code>login_required</code> decorator does not check. Instead, the wizard tests rely on <code>APP_ENV=test</code> falling through the <code>development</code> bypass, so routes are accessible without a session.</p> <p>Note</p> <p>Check <code>web/auth.py</code> if this behaviour changes — the bypass condition is <code>APP_ENV == \"development\"</code>. Tests that need authenticated routes should set <code>session[\"logged_in\"] = True</code> via <code>client.session_transaction()</code>.</p>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#testing-session-backed-state","level":3,"title":"Testing session-backed state","text":"<p>Use <code>app.test_request_context()</code> to get a real Flask request context with an active session:</p> <pre><code>def test_mark_connection_tested(app):\n    with app.test_request_context():\n        from flask import session\n        from web.setup_state import mark_connection_tested\n        mark_connection_tested()\n        assert session.get(\"setup_connection_tested\") is True\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#asserting-redirect-destinations","level":3,"title":"Asserting redirect destinations","text":"<p>The wizard tests check <code>response.headers[\"Location\"]</code> rather than following redirects, which keeps assertions fast and explicit:</p> <pre><code>def test_redirects_to_setup_when_incomplete(client):\n    response = client.get(\"/dashboard/\", follow_redirects=False)\n    assert response.status_code == 302\n    assert \"/setup/\" in response.headers[\"Location\"]\n</code></pre>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/testing/#coverage","level":2,"title":"Coverage","text":"<p>Run with:</p> <pre><code>uv run pytest -m \"not functional\" \\\n  --cov=src \\\n  --cov-report=term-missing \\\n  --cov-report=html:htmlcov\n</code></pre> <p>Open <code>htmlcov/index.html</code> for a line-level breakdown. The two pre-existing failures in <code>test_main.py</code> (<code>test_main_runs_without_real_secrets</code>, <code>test_password_reader_called_with_correct_env_var</code>) are caused by a missing <code>pythonjsonlogger</code> install in the bare test environment — they pass when the full dependency set is available via <code>uv sync --extra dev</code>.</p>","path":["Developer guide","Testing"],"tags":[]},{"location":"developer/web-app/","level":1,"title":"Web application","text":"<p>The Flask application lives in <code>src/web/</code>. It is created by <code>create_app()</code> in <code>app.py</code> using the application factory pattern.</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#application-factory-apppy","level":2,"title":"Application factory — <code>app.py</code>","text":"<p><code>create_app(config=None)</code> registers all blueprints, attaches a context processor, and starts the scheduler.</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#context-processor","level":3,"title":"Context processor","text":"<p><code>inject_setup_status()</code> runs on every request and injects two variables into every template:</p> Variable Type Description <code>setup_status</code> <code>dict</code> Full output of <code>get_setup_status(app)</code> <code>setup_complete</code> <code>bool</code> Convenience alias for <code>setup_status[\"complete\"]</code> <p><code>base.html</code> uses <code>setup_complete</code> to choose between the wizard navbar and the operations navbar without any per-route logic.</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#authentication-authpy","level":2,"title":"Authentication — <code>auth.py</code>","text":"<p><code>login_required</code> is a decorator that:</p> <ul> <li>In development (<code>APP_ENV=development</code>) — passes through unconditionally.</li> <li>In production — redirects to <code>/login</code> if <code>session[\"logged_in\"]</code> is falsy.</li> </ul> <p>Login is a simple username/password check against <code>ADMIN_USER</code> and <code>ADMIN_PASSWORD</code> from the Flask config (set via environment variables).</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#blueprints","level":2,"title":"Blueprints","text":"Blueprint Prefix Gated by <code>auth</code> — — <code>setup</code> <code>/setup</code> <code>login_required</code> only <code>main</code> — <code>login_required</code> <code>config_editor</code> <code>/config</code> <code>login_required</code> <code>connections</code> <code>/connections</code> <code>login_required</code> <code>ssh_keys</code> <code>/ssh-keys</code> <code>login_required</code> <code>schedule</code> <code>/schedule</code> <code>login_required</code> + <code>setup_complete_required</code> (index, run-now, status only) <code>dashboard</code> <code>/dashboard</code> <code>login_required</code> + <code>setup_complete_required</code> <code>progress</code> <code>/progress</code> <code>login_required</code> + <code>setup_complete_required</code> <p><code>schedule.save</code> and <code>schedule.remove</code> are intentionally not gated by <code>setup_complete_required</code> — the wizard's Schedule step calls them before setup is complete.</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#live-progress-run_statepy-and-progresspy","level":2,"title":"Live progress — <code>run_state.py</code> and <code>progress.py</code>","text":"<p><code>RunState</code> is a module-level singleton (<code>state = RunState()</code>) shared between:</p> <ul> <li><code>scheduler.py</code> (writer) — calls <code>state.start()</code>, <code>state.add_line()</code>, <code>state.step_running()</code>, <code>state.step_done()</code>, <code>state.finish()</code>.</li> <li><code>progress.py</code> (reader) — calls <code>state.snapshot()</code> and <code>state.lines_from(seq)</code>.</li> </ul> <p>The SSE endpoint at <code>GET /progress/stream</code> generates an infinite stream that:</p> <ol> <li>Sends an initial <code>state</code> event with the current snapshot.</li> <li>Polls every 300 ms for new lines via <code>lines_from(seq)</code>.</li> <li>Pushes <code>line</code> events for each new log line, and a <code>state</code> event after each batch.</li> <li>Sends a final <code>state</code> + <code>done</code> event when the run finishes, then closes.</li> <li>Reconnects automatically after a 3-second back-off on network error.</li> </ol> <p>The browser client uses <code>EventSource</code> and handles <code>line</code>, <code>state</code>, and <code>done</code> event types.</p>","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#scheduler-schedulerpy","level":2,"title":"Scheduler — <code>scheduler.py</code>","text":"<p><code>get_scheduler()</code> returns a lazily-initialised <code>BackgroundScheduler</code> (APScheduler) singleton configured for UTC. The scheduler is started once at application boot via <code>init_scheduler(app)</code>.</p> <p><code>_run_backup(app)</code> is the job function. It:</p> <ol> <li>Calls <code>state.start()</code>.</li> <li>Attaches <code>_StateHandler</code> to the <code>backup_server</code> logger to bridge log records into <code>RunState</code>.</li> <li>Calls <code>backup_server.main.main()</code>.</li> <li>Removes the handler and calls <code>state.finish()</code>.</li> </ol> <p><code>_StateHandler</code> matches known log phrases to infer step transitions:</p> Phrase in log (case-insensitive) Action <code>start backup</code> <code>state.step_running(\"backup\")</code> <code>step 'backup' succeeded</code> <code>state.step_done(\"backup\", success=True)</code> <code>step 'backup' failed</code> <code>state.step_done(\"backup\", success=False)</code> (same for <code>retention</code> and <code>sync</code>)","path":["Developer guide","Web application"],"tags":[]},{"location":"developer/web-app/#dashboard-data-dashboard_datapy","level":2,"title":"Dashboard data — <code>dashboard_data.py</code>","text":"<p>Read-only queries against <code>log.db</code> returning plain dicts and lists. The routes stay thin — they call these functions and pass the results directly to <code>render_template</code>.</p> <p>Key functions:</p> Function Returns <code>get_summary(db_path)</code> Total/success/failed run counts, last run timestamp, task name list <code>get_task_stats(db_path)</code> Per-task run counts, success rate, last run timestamp <code>get_task_history(db_path, task_name, limit)</code> Paginated list of runs with step details","path":["Developer guide","Web application"],"tags":[]},{"location":"user/","level":1,"title":"User guide","text":"<p>This guide covers everything you need to get Mnemosynce running and keeping your data safe — from installation through day-to-day monitoring.</p>","path":["User guide"],"tags":[]},{"location":"user/#what-you-will-need","level":2,"title":"What you will need","text":"<p>Before you begin, make sure you have:</p> <ul> <li>A Linux server that will run Mnemosynce (the backup server)</li> <li>One or more machines whose data you want to back up (can include the backup server itself)</li> <li>A destination for the remote copy — another machine reachable over SSH, or a locally-mounted drive</li> <li>A Gmail account with an app password for email reports</li> </ul>","path":["User guide"],"tags":[]},{"location":"user/#steps-at-a-glance","level":2,"title":"Steps at a glance","text":"<ul> <li> Install Mnemosynce on your backup server</li> <li> Complete the first-time setup wizard in the web UI</li> <li> Write your backup configuration</li> <li> Generate SSH keys for passwordless access to remote hosts</li> <li> Test connectivity and set a schedule</li> <li> Watch your first run in the dashboard and progress view</li> </ul>","path":["User guide"],"tags":[]},{"location":"user/configuration/","level":1,"title":"Configuration","text":"<p>Mnemosynce is configured through a single YAML file (<code>backup_config.yml</code>) edited directly in the web UI or with any text editor.</p>","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#minimal-example","level":2,"title":"Minimal example","text":"<pre><code>dir_backup_local:  /mnt/backup/local\ndir_backup_remote: user@backup-host:/mnt/backup/remote\nemail_sender:      your.account@gmail.com\nemail_report:      you@example.com\n\ntasks:\n  - name: ServerData\n    dir_source: /data\n</code></pre>","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#full-example","level":2,"title":"Full example","text":"<pre><code># Where backups are staged locally\ndir_backup_local: /mnt/backup/local\n\n# Where the local backup is synced for offsite storage\n# Can be user@host:/path (SSH) or a local path\ndir_backup_remote: user@backup-host:/mnt/backup/remote\n\n# Gmail address used as the SMTP sender\nemail_sender: your.account@gmail.com\n\n# Who receives the status report\nemail_report: you@example.com\n\n# CC'd only when a task fails. Omit or set equal to email_report to disable.\nemail_admin: admin@example.com\n\ntasks:\n  - name: ServerData\n    dir_source: /data          # local path on this machine\n    excludes:\n      - downloads\n      - tmp\n      - \"*.log\"\n\n  - name: DesktopHome\n    dir_source: user@desktop:/home/user   # remote source over SSH\n    excludes:\n      - Downloads/*\n      - .cache\n      - .Trash\n      - \".DS_Store\"\n</code></pre>","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#top-level-fields","level":2,"title":"Top-level fields","text":"Field Required Description <code>dir_backup_local</code> Yes Local directory where task snapshots are stored <code>dir_backup_remote</code> Yes Remote destination for the final sync step. Use <code>user@host:/path</code> for SSH or an absolute path for a locally-mounted drive <code>email_sender</code> Yes Gmail address used as the From address and SMTP username <code>email_report</code> Yes Address that receives the status report after every run <code>email_admin</code> No Additional CC address for failure-only notifications. Omit or leave equal to <code>email_report</code> to disable <code>tasks</code> Yes List of one or more task definitions (see below)","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#task-fields","level":2,"title":"Task fields","text":"Field Required Description <code>name</code> Yes Unique label used in reports and dashboard <code>dir_source</code> Yes Source to back up. Local absolute path or <code>user@host:/path</code> for remote <code>excludes</code> No List of rsync-style glob patterns to exclude from the backup","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#source-path-formats","level":2,"title":"Source path formats","text":"LocalRemote (SSH) <pre><code>dir_source: /home/user\n</code></pre> <p>Backs up a directory on the same machine running Mnemosynce.</p> <pre><code>dir_source: user@hostname:/home/user\n</code></pre> <p>Backs up from a remote machine over SSH. The backup server's SSH key must be in <code>~/.ssh/authorized_keys</code> on the remote host. See SSH keys.</p>","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#retention-policy","level":2,"title":"Retention policy","text":"<p>Mnemosynce applies a fixed retention window — you cannot currently change it per-task. Snapshots older than any of these windows are deleted after each run:</p> Granularity Kept for Daily 7 days Weekly 4 weeks Monthly 12 months Yearly 5 years","path":["User guide","Configuration"],"tags":[]},{"location":"user/configuration/#email-password","level":2,"title":"Email password","text":"<p>The Gmail app password is never stored in the config file. Supply it through the environment:</p> DockerLocal / systemd docker-compose.yml<pre><code>environment:\n  GMAIL_PASSWORD: \"your-16-char-app-password\"\n</code></pre> <pre><code>echo \"your-app-password\" &gt; /etc/mnemosynce/gmail-password\nchmod 600 /etc/mnemosynce/gmail-password\nexport GMAIL_PASSWORD_FILE=/etc/mnemosynce/gmail-password\n</code></pre> <p>nix-sops users can point <code>GMAIL_PASSWORD_FILE</code> at the decrypted secret file.</p> <p>Generating a Gmail app password</p> <p>Go to your Google Account → Security → 2-Step Verification → App passwords. Generate one labelled \"Mnemosynce\". The password is a 16-character string with no spaces.</p>","path":["User guide","Configuration"],"tags":[]},{"location":"user/docker/","level":1,"title":"Docker deployment","text":"<p>This page covers production Docker deployment in more detail than the installation guide.</p>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#image","level":2,"title":"Image","text":"<p>The official image is built from <code>docker/Dockerfile</code> and published to the GitHub Container Registry:</p> <pre><code>ghcr.io/mark-me/backup-server:latest\n</code></pre> <p>The image is based on <code>ghcr.io/astral-sh/uv:python3.13-alpine</code> and includes:</p> <ul> <li>Python 3.13 (via uv)</li> <li><code>rsync</code> — used by <code>backup.sh</code> and <code>sync_backup_to_remote.sh</code></li> <li><code>openssh-client</code> — <code>ssh</code>, <code>ssh-keygen</code> for key management and remote checks</li> <li><code>bash</code> — the shell scripts require bash, not sh</li> </ul>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#volume-mount","level":2,"title":"Volume mount","text":"<p>The single volume <code>/data</code> holds all persistent state:</p> <pre><code>/data/\n├── backup_config.yml   ← your backup configuration\n├── log.db              ← SQLite run history\n├── schedule.json       ← saved cron schedule\n└── ssh/                ← generated SSH keypairs\n</code></pre> <p>Mount your own directory here:</p> <pre><code>volumes:\n  - ./mnemosynce-data:/data\n</code></pre> <p>Do not lose this directory</p> <p>All configuration, run history, and SSH keys live here. Back it up, or at minimum keep your <code>backup_config.yml</code> in version control.</p>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#environment-variables","level":2,"title":"Environment variables","text":"Variable Required Description <code>SECRET_KEY</code> Yes (production) Flask session secret — use a long random string <code>ADMIN_USER</code> No Web UI username (default: <code>admin</code>) <code>ADMIN_PASSWORD</code> Yes (production) Web UI password <code>GMAIL_ADDRESS</code> Yes Gmail sender address <code>GMAIL_PASSWORD</code> Yes Gmail app password <code>DATA_ROOT</code> No Override the data directory (default: <code>/data</code>) <code>APP_ENV</code> No <code>production</code> (default in Docker) or <code>development</code>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#reverse-proxy-nginx","level":2,"title":"Reverse proxy (nginx)","text":"<p>To expose Mnemosynce over HTTPS behind nginx:</p> <pre><code>server {\n    listen 443 ssl;\n    server_name backup.example.com;\n\n    ssl_certificate     /etc/ssl/certs/backup.example.com.crt;\n    ssl_certificate_key /etc/ssl/private/backup.example.com.key;\n\n    location / {\n        proxy_pass         http://localhost:5000;\n        proxy_set_header   Host $host;\n        proxy_set_header   X-Real-IP $remote_addr;\n\n        # Required for Server-Sent Events (live progress view)\n        proxy_buffering    off;\n        proxy_cache        off;\n        proxy_read_timeout 3600s;\n    }\n}\n</code></pre> <p>SSE and proxy buffering</p> <p>The live progress view uses Server-Sent Events. If your proxy buffers responses, the terminal feed will appear frozen. Set <code>proxy_buffering off</code> and <code>proxy_read_timeout</code> to at least 60 seconds.</p>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#github-actions-deployment","level":2,"title":"GitHub Actions deployment","text":"<p>The scaffolded workflow at <code>.github/workflows/docs.yml</code> deploys the documentation site to GitHub Pages. For the application itself, add a step that pushes to your server via SSH or a container registry on each release.</p>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/docker/#updating","level":2,"title":"Updating","text":"<pre><code>docker compose pull\ndocker compose up -d\n</code></pre> <p>The <code>/data</code> volume is preserved across image updates.</p>","path":["User guide","Docker deployment"],"tags":[]},{"location":"user/installation/","level":1,"title":"Installation","text":"<p>Mnemosynce is distributed as a Python package and runs inside Docker in production. Pick the path that suits your setup.</p>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#option-a-docker-recommended-for-production","level":2,"title":"Option A — Docker (recommended for production)","text":"<p>Docker is the simplest way to run Mnemosynce on a home server. It bundles Python, <code>rsync</code>, <code>ssh</code>, and the web UI into a single image.</p>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#prerequisites","level":3,"title":"Prerequisites","text":"<ul> <li>Docker and Docker Compose installed on your backup server</li> <li>Port 5000 available (or change the mapping below)</li> </ul>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#docker-composeyml","level":3,"title":"docker-compose.yml","text":"<p>Create a <code>docker-compose.yml</code> wherever you want to manage the service:</p> <pre><code>services:\n  mnemosynce:\n    image: ghcr.io/mark-me/backup-server:latest\n    restart: unless-stopped\n    ports:\n      - \"5000:5000\"\n    volumes:\n      - /mnt/backup:/mnt/backup       # your backup storage\n      - ./data:/data                  # config, database, SSH keys\n    environment:\n      SECRET_KEY: \"change-me-to-a-random-string\"\n      ADMIN_USER: \"admin\"\n      ADMIN_PASSWORD: \"change-me\"\n      GMAIL_ADDRESS: \"your.account@gmail.com\"\n      GMAIL_PASSWORD: \"your-16-char-app-password\"\n</code></pre> <p>Start it:</p> <pre><code>docker compose up -d\n</code></pre> <p>Open <code>http://&lt;your-server&gt;:5000</code> in a browser. You will land on the setup wizard.</p> <p>Change the defaults</p> <p>The container refuses to start in production if <code>SECRET_KEY</code> or <code>ADMIN_PASSWORD</code> are left as the placeholder strings. Set them to strong, unique values before going live.</p>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#option-b-local-python-install","level":2,"title":"Option B — Local Python install","text":"<p>Use this for development or if you prefer not to use Docker.</p>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#prerequisites_1","level":3,"title":"Prerequisites","text":"<ul> <li>Python 3.12 or newer</li> <li><code>uv</code> package manager</li> <li><code>rsync</code> and <code>openssh-client</code> available on the system</li> </ul> <pre><code># Install uv if you don't have it\ncurl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#clone-and-install","level":3,"title":"Clone and install","text":"<pre><code>git clone https://github.com/mark-me/backup-server.git\ncd backup-server\nuv sync\n</code></pre>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#configure-environment","level":3,"title":"Configure environment","text":"<p>Copy the example file and fill in your values:</p> <pre><code>cp .env.example .env\n</code></pre> .env<pre><code>APP_ENV=development        # use \"production\" on a real server\nSECRET_KEY=change-me\nADMIN_USER=admin\nADMIN_PASSWORD=change-me\nGMAIL_ADDRESS=your.account@gmail.com\nGMAIL_PASSWORD=your-app-password\n</code></pre>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#start-the-web-server","level":3,"title":"Start the web server","text":"<pre><code>uv run flask --app src/web/app:create_app run --host 0.0.0.0 --port 5000\n</code></pre> <p>For production use Gunicorn instead:</p> <pre><code>uv run gunicorn \\\n  --bind 0.0.0.0:5000 \\\n  --workers 2 \\\n  --timeout 120 \\\n  \"web.app:create_app()\"\n</code></pre>","path":["User guide","Installation"],"tags":[]},{"location":"user/installation/#data-directory","level":2,"title":"Data directory","text":"<p>Both install methods use a single persistent directory (<code>/data</code> in Docker, <code>dev-data/</code> locally) that holds:</p> File Purpose <code>backup_config.yml</code> Your backup task definitions <code>log.db</code> SQLite database of every run <code>schedule.json</code> Saved cron schedule <code>ssh/</code> Generated SSH keypairs <p>Mount or back up this directory — it is everything Mnemosynce needs to survive a restart.</p>","path":["User guide","Installation"],"tags":[]},{"location":"user/monitoring/","level":1,"title":"Monitoring","text":"<p>Mnemosynce gives you two live views and a full history log to keep tabs on your backups.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#dashboard","level":2,"title":"Dashboard","text":"<p>Navigation → Dashboard</p> <p>The dashboard is the default home after setup. It shows:</p> <ul> <li>Summary cards — total runs, successful runs, failed runs, time of last run</li> <li>Per-task table — run counts, success rate progress bar, last run timestamp</li> <li>Recent runs — the five most recent task runs with step-level badges (bak / ret / syn)</li> </ul>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#readiness-strip","level":3,"title":"Readiness strip","text":"<p>When any of the four setup checks are still incomplete (config, SSH key, connection tested, schedule), a warning strip appears at the top of the dashboard with badge indicators and a direct link back to the relevant setup step.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#full-history","level":3,"title":"Full history","text":"<p>Click Full history (top right of the dashboard) or the clock icon next to any task to see a paginated, filterable log of every run. Expand any row to see step-by-step timing and source/destination paths.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#progress-view","level":2,"title":"Progress view","text":"<p>Navigation → Progress</p> <p>The progress view shows a live terminal feed of the current or most recent backup run using Server-Sent Events (SSE). The page updates automatically without any polling delay.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#status-indicators","level":3,"title":"Status indicators","text":"Badge Meaning <code>Running</code> (pulsing blue) A backup is in progress <code>Completed</code> (green) Last run finished successfully <code>Failed</code> (red) Last run encountered an error <code>No run yet</code> (grey) No backup has been run <p>Step badges (backup, retention, sync) show the state of each individual step in the same colour scheme.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#log-output","level":3,"title":"Log output","text":"<p>The terminal panel streams log lines colour-coded by level:</p> Colour Level White Info Yellow Warning Red Error Grey Debug <p>Use the autoscroll toggle (↓) to pause scrolling when inspecting earlier output. Click the trash icon to clear the display (does not delete the underlying logs).</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/monitoring/#email-reports","level":2,"title":"Email reports","text":"<p>After every scheduled or manual run, Mnemosynce sends an HTML email to <code>email_report</code> summarising:</p> <ul> <li>Which tasks ran and whether they succeeded</li> <li>Elapsed time per task and per step</li> <li>Days since last successful run per task</li> <li>Attached log files for any failed steps</li> </ul> <p>If <code>email_admin</code> is set to a different address from <code>email_report</code>, that address is CC'd on failure-only emails.</p>","path":["User guide","Monitoring"],"tags":[]},{"location":"user/scheduling/","level":1,"title":"Scheduling","text":"<p>Backups can run on a fixed schedule using a standard cron expression, or triggered manually from the web UI at any time.</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/scheduling/#setting-a-schedule","level":2,"title":"Setting a schedule","text":"<p>Go to Schedule in the navigation bar (or the Schedule step of the setup wizard).</p> <p>Enter a five-field cron expression or pick a preset:</p> Preset Expression Description Daily 02:00 <code>0 2 * * *</code> Every night at 02:00 UTC Weekly Sun 03:00 <code>0 3 * * 0</code> Sunday nights Monthly 1<sup>st</sup> 04:00 <code>0 4 1 * *</code> First day of each month Every 6 hours <code>0 */6 * * *</code> Four runs per day <p>Check Enable automatic scheduling and click Save schedule. The next run time is shown immediately.</p> <p>All times are UTC</p> <p>The scheduler runs in UTC. If you are in a UTC+2 timezone and want a 02:00 local run, use <code>0 0 * * *</code>.</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/scheduling/#cron-expression-reference","level":2,"title":"Cron expression reference","text":"<pre><code>┌───────── minute  (0–59)\n│ ┌─────── hour    (0–23)\n│ │ ┌───── day     (1–31)\n│ │ │ ┌─── month   (1–12)\n│ │ │ │ ┌─ weekday (0–6, Sun=0)\n│ │ │ │ │\n0 2 * * *\n</code></pre> <p>Special characters: <code>*</code> any, <code>,</code> list, <code>-</code> range, <code>/</code> step.</p> <p>Use crontab.guru to check an expression before saving.</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/scheduling/#triggering-a-manual-run","level":2,"title":"Triggering a manual run","text":"<p>Click Run now on either the Schedule page or the Progress page. The backup starts immediately in a background thread and you are redirected to the Progress view to watch it live.</p> <p>Info</p> <p>A manual run does not interfere with the cron schedule. If a scheduled run is due while a manual run is still in progress, the scheduled run will be skipped (misfire grace period: 5 minutes).</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/scheduling/#disabling-the-schedule","level":2,"title":"Disabling the schedule","text":"<p>On the Schedule page, uncheck Enable automatic scheduling and click Save schedule. The schedule configuration is preserved so you can re-enable it later without retyping the expression.</p> <p>Click Remove to delete the schedule entirely.</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/scheduling/#schedule-persistence","level":2,"title":"Schedule persistence","text":"<p>The schedule is stored in <code>DATA_ROOT/schedule.json</code> and reloaded automatically when the server starts. Restarting Docker or the process does not lose the schedule.</p>","path":["User guide","Scheduling"],"tags":[]},{"location":"user/setup-wizard/","level":1,"title":"First-time setup","text":"<p>When you open Mnemosynce for the first time — or whenever the setup checks are incomplete — you are guided through a four-step wizard before reaching the dashboard. You cannot skip to the monitoring views until setup is finished (or you click Finish setup anyway).</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#the-four-steps","level":2,"title":"The four steps","text":"<pre><code>flowchart LR\n    A[1 · Configuration] --&gt; B[2 · SSH Keys]\n    B --&gt; C[3 · Connections]\n    C --&gt; D[4 · Schedule]\n    D --&gt; E([Dashboard])\n    style E fill:#1D6A39,color:#fff,stroke:none</code></pre> <p>Local-only sources skip steps 2 and 3</p> <p>If every backup source in your configuration is a local path (no <code>user@host:</code> syntax), the SSH Keys and Connections steps are skipped automatically — you go straight from Configuration to Schedule.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#step-1-configuration","level":3,"title":"Step 1 — Configuration","text":"<p>Write your backup configuration in YAML. A template is pre-loaded to get you started. See the Configuration reference for every available field.</p> <p>Click Save configuration to validate and persist the file. The Next button appears once the file is saved and valid.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#step-2-ssh-keys","level":3,"title":"Step 2 — SSH Keys","text":"<p>Generate an ed25519 keypair for each remote host you need to access. After generating a key:</p> <ol> <li>Copy the public key displayed on screen.</li> <li>Paste it into <code>~/.ssh/authorized_keys</code> on the remote host.</li> </ol> <p>You can generate as many keys as you need. The private keys never leave the server.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#step-3-connections","level":3,"title":"Step 3 — Connections","text":"<p>Test that the server can reach its sources and send email.</p> Test What it checks SSH Ping → SSH login → (optionally) directory existence Email SMTP reachability → Gmail login → test message sent <p>Run at least one successful test. The step is marked complete and the Next button activates. If you cannot get a test to pass, click Skip for now and fix it later from Settings → Connections.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#step-4-schedule","level":3,"title":"Step 4 — Schedule","text":"<p>Set a cron expression to run backups automatically. Use the quick-preset buttons or type your own expression.</p> Preset Expression Runs Daily 02:00 <code>0 2 * * *</code> Every night at 02:00 UTC Weekly Sun 03:00 <code>0 3 * * 0</code> Sunday nights at 03:00 UTC Monthly 1<sup>st</sup> 04:00 <code>0 4 1 * *</code> First of each month at 04:00 UTC Every 6 hours <code>0 */6 * * *</code> Four times a day <p>Enable the Enable automatic scheduling checkbox and click Save schedule.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#finishing-setup","level":2,"title":"Finishing setup","text":"<p>Setup completes via either path — whichever comes first:</p> <ul> <li>Automatic — all four checks pass (config saved, SSH key present, connection tested, schedule enabled). The wizard detects this and shows a \"Go to dashboard\" button.</li> <li>Manual — click Finish setup anyway on the Schedule step at any time. This sets a session flag that lets you through even if some checks are still pending.</li> </ul> <p>Session-scoped state</p> <p>The connection-test result is stored in your browser session. If the server restarts before setup is complete, you will return to the wizard and need to re-run the connection test. All other checks (config, SSH key, schedule) are re-derived from disk on every request.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/setup-wizard/#after-setup","level":2,"title":"After setup","text":"<p>Once setup is complete, the navigation changes:</p> <ul> <li>Before setup — navbar shows the four wizard steps with amber indicators on incomplete ones.</li> <li>After setup — navbar shows Dashboard, Progress, Schedule, and a Settings dropdown (Configuration, SSH Keys, Connections).</li> </ul> <p>The home route (<code>/</code>) redirects permanently to the Dashboard.</p>","path":["User guide","First-time setup"],"tags":[]},{"location":"user/ssh-keys/","level":1,"title":"SSH keys","text":"<p>Mnemosynce uses SSH keys for two distinct purposes. Understanding which key does what will save you time when setting things up.</p>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#two-key-roles","level":2,"title":"Two key roles","text":"Key Used for Where it lives Source key Pulling data from remote source machines (e.g. your desktop) Generated in the web UI, stored in <code>/data/ssh/</code> Remote sync key Pushing the local backup to the remote storage host Hardcoded path: <code>/root/.ssh/id_ed25519_backup</code>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#source-keys-managed-in-the-web-ui","level":2,"title":"Source keys — managed in the web UI","text":"<p>Go to Settings → SSH Keys (or the SSH Keys step of the setup wizard).</p>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#generating-a-key","level":3,"title":"Generating a key","text":"<ol> <li>Enter a name — letters, numbers, hyphens, and underscores only (e.g. <code>desktop_mark</code>).</li> <li>Optionally add a comment (e.g. <code>backup-server/desktop_mark</code>).</li> <li>Click Generate.</li> </ol> <p>The private key is saved to <code>/data/ssh/&lt;name&gt;</code> with mode <code>600</code>. The public key is shown immediately.</p>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#copying-the-public-key-to-a-remote-host","level":3,"title":"Copying the public key to a remote host","text":"<p>After generating, copy the public key string displayed on screen and paste it into <code>~/.ssh/authorized_keys</code> on the remote machine:</p> <pre><code># On the remote machine (e.g. your desktop)\necho \"ssh-ed25519 AAAA...rest-of-key...\" &gt;&gt; ~/.ssh/authorized_keys\n</code></pre> <p>Or use <code>ssh-copy-id</code> from the backup server if you still have password access:</p> <pre><code>ssh-copy-id -i /data/ssh/desktop_mark.pub user@desktop\n</code></pre>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#testing-the-key","level":3,"title":"Testing the key","text":"<p>After copying the public key, go to Settings → Connections and run an SSH test with the same <code>user@host</code> pair. A successful test confirms the key is trusted.</p>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#remote-sync-key-manual-setup","level":2,"title":"Remote sync key — manual setup","text":"<p>The <code>sync_backup_to_remote.sh</code> script always uses the key at <code>/root/.ssh/id_ed25519_backup</code>. Generate it once on the backup server:</p> <pre><code>ssh-keygen -t ed25519 -f /root/.ssh/id_ed25519_backup -N \"\"\nssh-copy-id -i /root/.ssh/id_ed25519_backup.pub user@backup-host\n</code></pre> <p>This key is not managed by the web UI and is not stored in <code>/data/ssh/</code>.</p>","path":["User guide","SSH keys"],"tags":[]},{"location":"user/ssh-keys/#deleting-a-key","level":2,"title":"Deleting a key","text":"<p>On the SSH Keys page, click Delete next to any key. Both the private and public key files are removed. If a backup task references that key's host, the backup step will fail until a new key is generated and installed.</p> <p>Warning</p> <p>Deleting a key is irreversible. If you delete a key that is in active use, the corresponding backup task will fail at its next run.</p>","path":["User guide","SSH keys"],"tags":[]}]}