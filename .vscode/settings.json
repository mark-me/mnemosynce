{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "backup-logdb",
            "database": "${workspaceFolder:backup-server}/log.db"
        }
    ]
}