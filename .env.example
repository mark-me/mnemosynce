# Copy this to .env for local development. Never commit .env.
APP_ENV=development
SECRET_KEY=change-me-locally
ADMIN_USER=admin
ADMIN_PASSWORD=dev-password
GMAIL_ADDRESS=your.account@gmail.com
GMAIL_PASSWORD=your-app-password
# DATA_ROOT defaults to dev-data/ in the project root when APP_ENV=development
